const SB_URL='https://scqezmnmdwtngvvunfsb.supabase.co',SB_KEY='sb_publishable_iTzfZf9H5P0flPzyJOEjrQ_1Y1BHjW_';function vid(){let i=localStorage.getItem('bl_vid');if(!i){i=crypto.randomUUID?crypto.randomUUID():Math.random().toString(36).slice(2)+Date.now();localStorage.setItem('bl_vid',i)}return i}function params(){const p=new URLSearchParams(location.search),saved=JSON.parse(sessionStorage.getItem('bl_attrib')||'{}');const source=(p.get('utm_source')||p.get('src')||saved.source||'').slice(0,100),campaign=(p.get('utm_campaign')||p.get('campaign')||saved.campaign||null);if(source||campaign)sessionStorage.setItem('bl_attrib',JSON.stringify({source,campaign}));return{source,campaign}}function src(){const a=params();if(a.source)return a.source;try{let h=document.referrer?new URL(document.referrer).hostname:'';return h?h.replace(/^www\./,''):'direct'}catch(e){return'direct'}}function dev(){return innerWidth<768?'mobile':innerWidth<1100?'tablet':'desktop'}async function blTrack(event_type,e={}){const a=params(),b={event_type,page_path:location.pathname.slice(0,200),destination:e.destination||null,placement:e.placement||null,source:src(),campaign:a.campaign,referrer_host:null,device_type:dev(),visitor_id:vid()};try{await fetch(SB_URL+'/rest/v1/beardlaws_link_events',{method:'POST',headers:{apikey:SB_KEY,Authorization:'Bearer '+SB_KEY,'Content-Type':'application/json',Prefer:'return=minimal'},body:JSON.stringify(b),keepalive:true})}catch(e){}}document.addEventListener('DOMContentLoaded',()=>{blTrack('page_view');document.querySelectorAll('[data-track]').forEach(el=>el.addEventListener('click',()=>blTrack('outbound_click',{destination:el.dataset.track,placement:el.dataset.placement||'unknown'})))})